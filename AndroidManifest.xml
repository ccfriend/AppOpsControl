<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zte.appopscontrol"
    android:versionCode="1"
    android:versionName="1.0"
    android:sharedUserId="android.uid.system">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".privacyguard.PrivacySettings" />
        <activity android:name=".privacyguard.PrivacyGuardManagerActivity" />
        <activity android:name=".applications.AppOpsSummaryActivity" />
        
        <activity android:name=".AppOpsControlActivity" />
        <activity android:name=".PermToAppsActivity" />

    </application>

</manifest>
